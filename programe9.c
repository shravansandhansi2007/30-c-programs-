# include <stdio.h>
int main()
{
    int a;
    float area;
     printf("Enter the length of square:");
    scanf("%d", &a);
    area =a*a;
    printf("area of square is %2f", area);

  return 0;
}